# Copy this file to .env (gitignored) and fill in your Pi's actual
# tailscale IP — find it by running `tailscale ip -4` on the Pi.
# docker-compose.yml uses this to publish the app's port ONLY on the
# tailnet interface, never on 0.0.0.0/the public LAN.
TAILSCALE_IP=100.x.y.z
